# Copy this to backend/.env file

# MongoDB
MONGODB_URI=mongodb://localhost:27017/testflow

# Server
API_PORT=3000
NODE_ENV=development

# GitHub API
GITHUB_TOKEN=ghp_your_github_token_here

# AI Services
GEMINI_API_KEY=your_gemini_api_key_here
MINIMAX_API_KEY=your_minimax_api_key_here

# CodeRabbit (optional)
CODERABBIT_API_KEY=
CODERABBIT_WEBHOOK_SECRET=

# Jira API
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your_jira_api_token_here

# Jira Project Configuration
# Default project key for creating standalone issues when parent ticket doesn't exist
# Example: "TEST", "DEMO", "PROJ"
JIRA_PROJECT_KEY=TEST

# Jira Mock Mode (for development/demo only)
# Set to "true" to use mock data when Jira API fails (creates fake tickets)
# Set to "false" to return real errors (recommended for production)
JIRA_USE_MOCK=false

# Jira Webhook Security (Optional)
# Get this from Jira webhook settings when creating webhook
# If not set, webhook signature verification is disabled (for development)
JIRA_WEBHOOK_SECRET=your_webhook_secret_here

